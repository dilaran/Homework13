<h1>In-da-Burger</h1>
<img src="/t-mcdonalds-qpc-deluxe-burger.jpg"  width="460" height="345">
<h2>Devoured</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burgers-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>New burger</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burgers-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">add?</label><br>
    <input type="radio" name="addnew" value="1" checked> add new delicious burger<br>
  
  </div>

  <button type="submit">Add burger</button>
</form>
